# Railway configuration for the Next.js frontend
# Uses Nixpacks to build and start Next.js

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start -- -p $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
